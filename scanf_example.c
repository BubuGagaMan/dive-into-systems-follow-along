#include <stdio.h>

int main(int argc, char *argv[]) {
	// pre-declaring the variables in which input will be stored
	int num1;

	printf("Enter an integer: \n");
	// & used to store into num1
	scanf("%d", &num1);

	printf("Your number: %d", num1);
   
	 return 0;
}
